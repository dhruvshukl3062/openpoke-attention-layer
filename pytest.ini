[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
addopts = -q --strict-markers
markers =
    known_bug: pins a defect that exists in upstream OpenPoke; flips to a pass when fixed.
filterwarnings =
    ignore::DeprecationWarning
